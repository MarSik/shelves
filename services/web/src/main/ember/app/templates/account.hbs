<div class="row">
    <section class="columns large-6">
        <h3>Logged in user</h3>

        <dl>
            <dt>Name</dt>
            <dd>{{model.name}}</dd>
        </dl>

        <dl>
            <dt>Email</dt>
            <dd>{{model.email}}</dd>
        </dl>

        <dl>
            <dt>ID</dt>
            <dd>{{model.id}}</dd>
        </dl>
    </section>

    <section class="columns large-6">
        <h3>Pairing token</h3>

        <p>{{session.content.access_token}}</p>

        <p>{{fa-icon 'android' size=2}} Pair your Android application by scanning the code below:</p>

        <p>{{view 'qrcodejs' url=tokenQrUrl size=256}}</p>

        <h3>Authorizations</h3>

        <p>This is a list of paired devices that have access to your account.</p>

        <ul>
            {{#each auth in model.authorizations}}
                <li>{{auth.name}} {{#confirm-button class='remove button' action='removeAuthorization' param=auth}}{{fa-icon 'times'}}{{/confirm-button}}</li>
            {{/each}}
        </ul>
    </section>
</div>

{{outlet}}
