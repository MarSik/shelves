<form {{action 'download' on='submit'}}>
<div class="row collapse">
    <div class="small-10 columns">
        {{input type="text" placeholder="Please enter the url to download" value=url}}
    </div>
    <div class="small-2 columns">
        <button type="submit" class="button postfix" {{bind-attr disabled=disabled}}>{{fa-icon 'cloud-download'}} Download</a>
    </div>
</div>
</form>
{{yield}}
