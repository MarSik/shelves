import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:file-input', 'FileInputView');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
