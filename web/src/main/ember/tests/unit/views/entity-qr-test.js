import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:entity-qr', 'EntityQrView');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
